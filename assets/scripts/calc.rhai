// assets/scripts/calc.rhai

let app_id   = "com.soulos.calc";
let app_name = "Calc";
let app_icon = "calc";

fn on_draw() {
    title_bar(app_name);
    label(10, 80, "Coming soon.");
}

fn on_event(ev) {}
