// assets/scripts/sync.rhai

let app_id   = "com.soulos.sync";
let app_name = "Sync";
let app_icon = "sync";

fn on_draw() {
    title_bar(app_name);
    label(10, 80, "Coming soon.");
}

fn on_event(ev) {}
